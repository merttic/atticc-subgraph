{
  "name": "atticc-mono",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ --ipfs http://localhost:5001 atticc-mono",
    "create-local": "graph create --node http://localhost:8020/ atticc-mono",
    "remove-local": "graph remove --node http://localhost:8020/ atticc-mono",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 atticc-mono",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.45.2",
    "@graphprotocol/graph-ts": "^0.29.3"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
